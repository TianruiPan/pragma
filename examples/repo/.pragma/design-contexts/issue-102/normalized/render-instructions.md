# Render Instructions

## Pixel Contract
- Use normalized/pixel-spec.json as the primary pixel implementation contract.
- Preserve bounds, z-index order, text styles, fills, strokes, radii, shadows, opacity, and asset bindings unless the target product design system requires a documented equivalent.

## Required Assets
- asset-drone-icon

## Dynamic / Non-pixel Regions
- region-dynamic-1: implementation-defined; The center map uses the production map service. Treat the Figma map as an intent reference for control placement, visual balance, and overlay density.

## Notes
The center map uses the production map service. Treat the Figma map as an intent reference for control placement, visual balance, and overlay density.
